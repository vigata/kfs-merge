
this library merges instances of a JSON schema using predentermined rules. 

Example usage:

Given a JSON schema, an two instances A and B of that schema, and a set of rules, this library will merge A and B into a new instance C.



References:
